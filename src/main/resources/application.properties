# ✅ Spring Application Name
spring.application.name=productservice

# ✅ Database Configuration (Local MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/agribzar2?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Sai@9912
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ✅ Hibernate & JPA Configuration (Local MySQL)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.defer-datasource-initialization=true

# ✅ Logging Configuration (for debugging)
logging.level.org.springframework.web.client.RestTemplate=DEBUG
logging.level.org.springframework=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ✅ Eureka Client Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true

# ✅ Server Configuration
server.port=9099

# ✅ Database Configuration (Railway Database)
# Uncomment the section below if you want to use the Railway database

# spring.datasource.url=jdbc:mysql://yamabiko.proxy.rlwy.net:17624/railway
# spring.datasource.username=root
# spring.datasource.password=SueGQtHPsWAzUdoYMTexpmgwWurCumTg
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ✅ JPA Hibernate Configuration (Railway Database)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# spring.jpa.show-sql=true

# ✅ Bind to Railway Port (for Railway deployment)
# Fallback to 8080 if PORT environment variable is not set
